// Wrapper - re-export app from existing backend source (keeps original code in src/)
export { app } from '../src/app.js';

// Note: The real implementation remains in `src/app.js` to avoid risky large moves.
// This wrapper provides the `backend/app.js` path you requested.
